<link rel="stylesheet" type="text/css" href="/user/styles/product_styles.css">
<link rel="stylesheet" type="text/css" href="/user/styles/product_responsive.css">
            <div class="single_product">
        <div id="content" class="clearfix">

            <div class="container">

                <div class="row">

                    <!-- *** LEFT COLUMN ***
             _________________________________________________________ -->

                    <div class="col-md-9" id="customer-orders">
                        <h3>Danh sách các đơn hàng</h3>
                        <p class="text-muted lead">Nếu có bất cứ thắc mắc nào, xin vui lòng <a href="contact.html">liên hệ chúng tôi</a>, hệ thống chăm sóc khách hàng luôn sẵn sàng 24/7.</p>
                             <table class="table table-bordered display" style="width:100%" id="user-order">
                                    <thead>
                                        <tr>
                                            <th>Mã số</th>
                                            <th>Ngày đặt</th>
                                            <th>Tổng cộng</th>
                                            <th>Tình trạng</th>
                                            <th>Thao tác</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {{#each products}}
                                            <tr>
                                                <th># {{this.id}}</th>
                                                <td>{{this.createddate}}</td>
                                                <td>$ {{this.total}}</td>
                                                {{#ifCond this.statusid "==" 2}}
                                                <td><span class="label label-info">{{this.status}}</span>
                                                </td>
                                                {{/ifCond}}
                                                {{#ifCond this.statusid "==" 3}}
                                                    <td><span class="label label-success">{{this.status}}</span>
                                                    </td>
                                                {{/ifCond}}
                                                {{#ifCond this.statusid "==" 0}}
                                                    <td><span class="label label-danger">{{this.status}}</span>
                                                    </td>
                                                {{/ifCond}}
                                                {{#ifCond this.statusid "==" 1}}
                                                    <td><span class="label label-warning">{{this.status}}</span>
                                                    </td>
                                                {{/ifCond}}
                                                <td><a href="user/order/{{this.id}}" class="btn btn-primary">Chi tiết</a>
                                                </td>
                                            </tr>
                                        {{/each}}
                                        
                                    </tbody>
                                </table>
                            <!-- /.table-responsive -->
                    </div>
                    {{> user}}

                </div>
                <!-- /.row -->

            </div>
            <!-- /.container -->
        </div>
        <!-- /#content -->

    </div>

    

